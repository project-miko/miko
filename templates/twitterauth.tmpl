{{ define "twitterauth.tmpl" }}

<!doctype html>
<html>
	<head>
		<meta charset="utf-8">
		<title>twitter auth</title>
	</head>
	<body>
		<div style="margin: 0 auto; margin-top: 50px; width: 400px; ">
            <h2>{{ .title }}</h2>
            <p>Twitter auth {{ .status }}</p>
            <p style="color: red;">{{ .errmsg }}</p>
            <p>Please close the webpage and refresh data</p>
			<!-- <p><button onclick="javascript:window.close();" style="width: 100px; height: 30px; border: none; border-radius: 8px; background: #1e80ff; color: white;">Close</button></p> -->
		</div>
	</body>
</html>

{{ end }}
